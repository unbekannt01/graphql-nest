# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

directive @upper on FIELD_DEFINITION

type LoginResult {
  message: String!
  access_token: String!
}

type Book {
  info: JSON!
  id: Int!
  title: String!
  price: Int!
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON

type User implements Person {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
  role: String!
  books: [Book!]
  files: [UploadFile!]
}

interface Person {
  id: ID!
  firstName: String!
  lastName: String!
}

type UploadFile {
  id: Int!
  filename: String!
  url: String!
  userId: Int!
}

type Query {
  index: String!
  securedDataforUser: String!
  securedDataforAdmin: String!
  login(email: String!, password: String!): LoginResult!
  users: [User!]!
  books: [Book!]!
  pagination(page: Int! = 1, limit: Int! = 10): [Book!]!
  myBooks: [Book!]!
  bookById(bookId: Int!): Book!
  myFiles: [UploadFile!]!
}

type Mutation {
  registerUser(registerUserArgs: RegisterUserArgs!): String!
  updateUser(updateUserArgs: UpdateUserArgs!): User!
  deleteUser(userId: Int!): String!
  deleteBook(bookId: Int!): String!
  addBook(addBookArgs: AddBookArgs!): String!
  updateBook(updateBookArgs: UpdateBookArgs!): Book!
  singleUpload(file: Upload!): String!
  deleteFile(fileId: Int!): String!
  updateFile(id: Int!, file: Upload!): String!
}

input RegisterUserArgs {
  firstName: String!
  lastName: String!
  email: String!
  password: String!
  role: String! = "USER"
}

input UpdateUserArgs {
  firstName: String
  lastName: String
  email: String
  password: String
  id: Int!
}

input AddBookArgs {
  id: Int!
  title: String!
  price: Int!
}

input UpdateBookArgs {
  title: String
  price: Int
  id: Int!
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload

type Subscription {
  bookAdded: Book!
}